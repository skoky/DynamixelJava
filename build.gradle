apply plugin: 'java'
apply plugin:'application'

mainClassName = "cz.skoky.xl320.Main"

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}

run {
    standardInput = System.in
}

